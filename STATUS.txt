SIMPLEBLUEPRINTS STATUS
Session: S84 (closed)
Cache buster: unchanged (no production-visible frontend change in S84)

TOP PRIORITIES (S85, in order):
1. tests/pdf/render_review.py harness (param sets -> PDFs -> 150dpi PNGs)
2. Fix 2.6ft position/render divergence; swap call sites to lotGeometry.js;
   Will visually verifies 10 Chichester + one tapered lot
3. Fuzz loop (crash + consistency oracles, incl. text-collision detector)
4. Golden-file PDF regression; then B1 elevation zone heights

READ FIRST: MASTER_CONTEXT.txt (capability playbook section 6 -- PDF visual
review + headless browser work IN CHAT, do not rediscover this).
PENDING ON WILL: confirm /admin login in prod; revoke classic PAT, mint
fine-grained replacement; answer open questions (MASTER_CONTEXT.txt sec 11).
BLOCKERS: none.
