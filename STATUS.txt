SIMPLEBLUEPRINTS STATUS
Session: S87 (closed clean; vision died after 1 view -- earliest yet; whole
session done via oracles + coordinate spies + Will's pasted screenshots)
Cache buster: s87a  (S87 push 4 bumped it for the zone-cap frontend changes)

SHIPPED S87 (pushes 1-11, all CI green):
1.  DECK LOADS table moved to a reserved band BELOW the house block on framing
    sheets (was printing ON TOP of the house -- Will caught it by eye; the old
    legibility oracle was blind to text-over-drawing). Scale bar moved below it.
2.  FILL THE SHEET (Option A, Will's pick): plan/framing panels print at the
    LARGEST STANDARD architect's scale that fits, content centered, and the
    scale label now states the TRUE printed scale. The old "1/4\"" label was
    FALSE (panels printed ~0.54 in/ft) -- a scale ruler would mis-measure every
    deck. 16x12 now prints true 1/2"=1'-0".
3.  Framing collision fixes: footing dims joined the corner dim string; house
    labels re-anchored to house bottom; stair DOWN slid off the OPENING dim.
    3-zone torture case 10 collisions -> 0.
4.  3-ZONE CAP, full stack: one shared constant (window.MAX_ADD_ZONES/atZoneCap),
    reducer guard, "+" handles hidden at cap, hint text, AI action guard + AI
    PROMPT told the limit, backend stays permissive for old saved projects.
5.  STANDING RULE added: FULL-STACK CONSISTENCY (MASTER_CONTEXT 9 + ARCHITECTURE).
6.  THREE PERMANENT ORACLES, all in CI:
    - text-over-linework detector (tests/pdf/linework_check.py) -- fixes the
      blind spot that hid #1; differential baseline, fonts+mpl pinned.
    - post-to-grade test (tests/test_post_grade.py) -- every pier sits ON the
      drawn grade; FOUND B7 + B8 on day one.
    - fuzz loop (tests/pdf/fuzz_configs.py) -- 60/60 configs clean; surfaced B9.

STATE: all suites green (2764/0 geometry, 52/0 structural, 50/0/1 future
products, post-grade 0 unexpected, linework at baseline, fuzz smoke clean).

THREE BUGS FOUND BY THE NEW ORACLES (none blind-fixed; see MASTER_CONTEXT
backlog B7/B8/B9): B7 piers float above bench pads on side slopes (+2.1ft);
B8 piers punch below the grade line on front-back slopes (-1.05ft) -- both need
a drawing-CONVENTION CALL from Will; B9 numeric frost/snow inputs silently
mis-interpreted (calc-vs-checker mismatch, no error) -- fixable directly.

THE BIG STRATEGIC POINT (Will, S87): the project's throughput bottleneck is
vision-dependence. The fix is NOT rationing vision -- it's NEEDING it less by
turning eye-judgments into oracles (each one then checks that whole class on
every push forever). See MASTER_CONTEXT 10D KEY INSIGHT + rewritten 6.4.

TOP PRIORITIES (S88) -- full list in MASTER_CONTEXT 11B:
1. [no vision] BUILD 3 MORE DETECTORS: text-outside-panel, text-clipped-at-edge,
   callout-into-neighbor-view. Same shape as linework_check.py. Highest leverage.
2. [no vision] FIX B9 (calc_engine input normalization).
3. [no vision] FIX B7/B8 -- but get Will's convention call FIRST.
4. [vision] CONFIG MATRIX with an INTERIOR-ANCHORED stair (B3 probe, still
   never run). Crash/oracle half needs no vision; only the final look does.
NOTE the S88 VISION-FIRST EXPERIMENT (6.4): if doing visual work, render+view
the needed sheets FIRST, before heavy context accumulates. Untried; report result.

JURISDICTION: COLORADO SPRINGS / PPRBD (partner Billy operates there). Reference
sets = All Things Architecture (CO Springs). is_colorado_springs() +
append_cos_attachment() exist; PPRBD attachment is the letter-size official form.

PENDING ON WILL: (a) bless the 3-zone framing sheet by eye + live zone-cap poke
(see MASTER_CONTEXT "PENDING WILL VISUAL GATES"); (b) B7/B8 convention call;
(c) Welborn street address (Ilaria = 4739 Sweetgrass Lane, CO Springs 80922 is
in hand); (d) contractor pricing ($49/$99?); (e) confirm PPRBD accepts a mixed-
size set (36x24 drawings + letter form). Will is KEEPING the classic PAT -- do
NOT re-raise revoking it. Real-address runs need live Realie/Overpass keys
(Railway), so run on simpleblueprints.xyz / headless live site, not bare sandbox.

BLOCKERS: none. NEXT SESSION: paste the prompt from S88_KICKOFF.txt.
