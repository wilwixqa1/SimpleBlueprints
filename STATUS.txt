SIMPLEBLUEPRINTS STATUS
Session: S88 (closed clean; VISION-FIRST EXPERIMENT SUCCEEDED -- 17/17 views,
zero drops. Strongest data yet for the context-size theory: front-loading the
visual review before heavy context bought a full visual pass with no outage.)
Cache buster: s87a  (unchanged -- S88 touched no production frontend)

SHIPPED S88 (pushes 1-4, all CI green):
1.  PANEL-OVERFLOW DETECTOR (tests/pdf/panel_check.py) -- G5 text-outside-panel,
    G6 clipped-at-edge (clip_on + sheet-trim), G7 callout-into-neighbor-view, one
    bbox scan, differential baseline (panel_baseline.json), fonts+mpl pinned+
    stamped, a --selftest that proves all three kinds on synthetic S85-bug shapes
    with no false positive. In CI. Drains inventory items G5/G6/G7 at once.
2.  B9 FIXED: resolve_frost_depth/resolve_snow_load in calc_engine are the single
    source of truth (number=literal inches/psf, category=table lookup). Routed
    calc_engine + permit_checker + permit_spec through them so calc-vs-checker
    defaults CANNOT diverge again. Steel load_case derived from resolved snow psf.
    Regression test (tests/test_frost_snow_normalize.py) locks calc==checker for
    both category AND numeric inputs incl. the exact 30-vs-36 silent mismatch.
    Gated on the 52 structural tests. In CI.
3.  CONFIG MATRIX (tests/pdf/config_matrix.py) -- first-ever exercise of the
    main-deck PARAMETRIC stair path (stairAnchorX/Y/Angle). 8 configs {0,1,2
    zones} x stair placements, each interior-anchored. Crash judge (full pipeline
    incl. permit checker) + numeric POST-IN-LANDING (B3/G11) check. In CI.
4.  ZONE-CAP LIVE GATE AUTOMATED (tests/live/zone_cap_dom.py) -- headless browser
    against a running server asserts the shipped window.atZoneCap/MAX_ADD_ZONES
    behave at the cap (false 0/2, true 3/4, cutouts excluded), the exact function
    planView edge handles + hint text gate on. Passed live against s87a. NOT in CI
    (runner has no chromium/Postgres); tests/live/README.txt has the run recipe.

STATE: all suites green (2764/0 geometry, 52/0 structural, 50/0/1 future
products, post-grade 64/0 unexpected, linework at baseline 25+41, panel at
baseline 13+16, fuzz smoke 8/0, config matrix 8/0 with B10 grandfathered).

NEW BUG FOUND (B10, by the config matrix's post-in-landing check): a deeply
interior-anchored main stair's LANDING overlaps the beam-post row -> a post is
drawn inside the landing. REALISTIC interior anchors (exit within ~1 ft of the
outer edge) stay clean; only the extreme overlap-the-beam case fires. Like
B7/B8 it needs a DESIGN CALL from Will (clamp the anchor so a landing can't
cross the beam line, or relocate the overlapped post), so it's recorded as a
KNOWN entry with regression sensitivity proven both ways; CI fails on any
deviation.

TWO S87 WILL-GATES, now both MACHINE-ADVANCED:
- 3-zone framing sheet: I VIEWED it this session (vision-first) -- reads clean,
  joist directions consistent, matches the oracle's 0 collisions. READY FOR
  WILL'S BLESS by eye.
- Live zone-cap poke: AUTOMATED and PASSED live (push 4). Residual manual bit is
  only a screenshot confirming the "+" handles are physically gone + the hint
  renders (the gating function is proven; the pixel render is not).

TOP PRIORITIES (S89) -- full list in MASTER_CONTEXT 11B:
1. [needs Will] B7/B8 convention call, THEN fix + zero the KNOWN table. Blocked
   on Will since S87.
2. [needs Will] B10 design call (clamp anchor vs relocate post), THEN fix + zero
   its KNOWN entry.
3. [no vision] REMAINING DETECTORS from 10E: G8 tiny-text (promote legibility's
   side signal), G9 garbled-overprint (check if tightened G1 covers it), G10
   dim-through-graphics (codify the existing fix). Same bbox/baseline shape.
4. [no vision] GOLDEN-FILE PDF regression (item 8), pin fonts+mpl.
NOTE the vision-first experiment WORKED -- keep front-loading any visual review.

JURISDICTION: COLORADO SPRINGS / PPRBD (partner Billy operates there). Reference
sets = All Things Architecture (CO Springs). is_colorado_springs() +
append_cos_attachment() exist; PPRBD attachment is the letter-size official form.

PENDING ON WILL: (a) bless the 3-zone framing sheet by eye (I viewed it, reads
clean); (b) B7/B8 convention call; (c) B10 design call (NEW this session);
(d) Welborn street address (Ilaria = 4739 Sweetgrass Lane, CO Springs 80922 in
hand); (e) contractor pricing ($49/$99?); (f) confirm PPRBD accepts a mixed-size
set (36x24 drawings + letter form). Will is KEEPING the classic PAT -- do NOT
re-raise revoking it. Real-address runs need live Realie/Overpass keys (Railway),
so run on simpleblueprints.xyz / headless live site, not bare sandbox.

BLOCKERS: none. NEXT SESSION: paste the prompt from S88_KICKOFF.txt (update it
first if the vision-first ordering or the B7/B8/B10 gate list has changed).
