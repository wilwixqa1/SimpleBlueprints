SIMPLEBLUEPRINTS STATUS
Session: S85 (closed -- ended due to Claude vision outage; see below)
Cache buster: s85b

SHIPPED S85 (pushes 1-10, all CI green):
1-3. render_review harness (8 sets); divergence fix (renderer-space offset,
   live-verified via DOM); elevation grade fixes (sign inverted in all 4 PDF
   views; anchored to calc ref; side views anchor at depicted post); h=null
   zone crash fix; bench grade default; legibility oracle
4-6. STATUS/MASTER_CONTEXT/ARCHITECTURE updates; photoreal-3D logged deferred
7. UX: first wizard screen shows address form immediately (no intermediate
   button; chat demoted to optional) -- live DOM-verified
8. Quarantined stale S73 context file pushed by Will's PARALLEL session
   (docs/history/S73_MASTER_CONTEXT_stale.txt). GTM plan left at root.
9. Zone-stair world transform fixed: rotation-matrix port inverted left/right
   exit sides -- PDF drew zone stairs INSIDE zones; now mirrors planView.js
   txPt exactly. A-6 foundation-label collision fixed (was on EVERY config).
10. Legibility round 2: main dims clear stair+landing-pad extents; OPENING
   dims at deck edge; scale-bar caption fix; house label below dim band;
   detector threshold calibrated to 30%.
LEGIBILITY SCORE vs pro references (Loucks/Ilaria, in S85 chat + measured):
   refs = 1-2 genuine collisions/set. Ours: 4 sets at 0, 3 sets at 2,
   4-zone torture case at 10 (annotation crowding -- next).

!! VISUAL REVIEW GAP: pushes 9-10 verified by oracles + suites + CI ONLY.
   Claude's image viewing broke mid-S85 (likely context-budget cutoff of
   tool-result image payloads in a very long conversation; chat-uploaded
   images still worked). FIRST TASK S86: rasterize + eyeball pushes 9-10
   output (zones_stairs_lcr A-1, slope15 A-2), confirm with Will.

TOP PRIORITIES (S86):
1. Visual re-verification of pushes 9-10 (above) + Will's screenshot gate
2. Elevation ANNOTATION COLUMN: reserved right-margin text zone per view,
   leader lines, wrap not overflow (fixes Will's screenshot findings:
   callouts crossing panels, clipped mid-word, text over railing linework).
   Add detector checks: text-outside-panel, text-clipped-at-axes.
3. Sheet size decision (Will): 14x8.5in vs ARCH D 36x24 -- type is
   proportionally equal to refs; absolute print size is the gap
4. Config matrix battle-test: 1/2 zones x stairs main/zone/both/INTERIOR-
   ANCHORED (anchorX/Y path untested -- run FIRST); judges: crash + 143
   checks + legibility thresholds (<=2/set) + visual sample
5. 4-zone torture case annotation crowding (10 collisions)

NEXT SESSION: paste the prompt from S86_KICKOFF.txt to begin.
PENDING ON WILL: revoke classic PAT used S85; sheet size call; point the
parallel (GTM) session at current MASTER_CONTEXT.txt before it pushes code.
BLOCKERS: none (vision expected back in fresh session).
