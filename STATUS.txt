SIMPLEBLUEPRINTS STATUS
Session: S86 (closed clean; vision dropped ~10 views in, workaround used)
Cache buster: s85b  (S86 touched NO frontend -> no bump needed)

SHIPPED S86 (pushes 1-5, all CI green, HEAD 0675910):
1. Sheet size centralized in backend/drawing/sheet.py + render_scale() shim
   that scales fonts proportionally to sheet width. No-op at 14x8.5
   (verified 24/24 pages pixel-identical). Sizes: letter/tabloid/arch_c/
   arch_d; SBP_SHEET env or set_active() overrides.
2. Framing member callouts reflowed OFF the geometry into leaders.
3. COMPLEXITY BRANCH: decks with >=1 zone split DECK PLAN (A-1) and DECK
   FRAMING (A-2) onto separate FULL-WIDTH sheets; rest renumbered A-3..A-7.
   Sheet numbering data-driven in main.py; cover drawing-index + notes prose
   follow it. Simple (no-zone) decks keep combined A-1, proven byte-identical.
4. Cover drawing-index row height adapts to sheet count; DEFAULT SHEET SIZE
   FLIPPED TO ARCH D 36x24 (matches the CO Springs reference sets).
5. Zone-wing specs printed below each wing; beam group right-anchored tight
   to the drawing's left edge; DECK LOADS rebuilt as a titled table (green
   header, aligned cols, ruled total) anchored lower-left.

STATE: complex set zones_stairs_lcr = 0 text collisions across all 8 sheets.
Simple set = 0 collisions, byte-identical to pre-S86. Will approved plan +
framing + cover visually (via pasted screenshots; my view tool was down).

TOP PRIORITIES (S87) -- see MASTER_CONTEXT 11B for the full list:
1. FILL THE SHEET: small decks read small on 36x24; bump scale (3/8") for
   small decks and/or vertically center the drawing. Touches all sheets;
   needs working vision. TASTE CALL -> prototype + Will's pick.
2. BROAD 36x24 VISUAL PASS on the other sheets (elevations, notes, details,
   site, checklist, simple-deck versions) -- only plan/framing/cover were
   closely verified in S86.
3. MANY-ZONE scaling: verify zones_four_stairs_four; per-wing callouts may
   need a consolidated framing schedule at 4 zones.

JURISDICTION (corrected S86): target is COLORADO SPRINGS (partner Billy
operates there), NOT Brookhaven/Islip. The reference sets are CO Springs /
PPRBD. is_colorado_springs() + append_cos_attachment() already exist; the
PPRBD attachment is the official letter-size form appended as-is.

PENDING ON WILL: real test address -> Will gave two REAL CO Springs
addresses: Ilaria (4739 Sweetgrass Lane, Colorado Springs CO 80922; the wood-
deck reference set) and Welborn (steel-deck project; street address TBD from
Will). Contractor pricing ($49/$99?); confirm mixed page sizes (36x24 drawings
+ letter PPRBD form) are fine for PPRBD submission.
NOTE: Will is KEEPING the classic PAT (his decision -- do NOT re-raise
revoking it). A genuine real-address run needs the live Realie/Overpass APIs
(keys live on Railway), so it happens on simpleblueprints.xyz or a keyed env,
not the bare sandbox -- drive the live site headless (6.2) against these.
BLOCKERS: none. NEXT SESSION: paste the prompt from S87_KICKOFF.txt.
