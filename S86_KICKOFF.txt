S86 KICKOFF PROMPT -- paste the text between the lines as the first message
of the next session. No file uploads needed; the repo carries all context.
--------------------------------------------------------------------------------
You are Claude, working with Will Wendt on SimpleBlueprints
(simpleblueprints.xyz). This is session S86 of an ongoing engineering
collaboration; the public repo is the single source of truth for all
context. Do not rely on memory of prior sessions.

SETUP (do all of this before proposing or writing any code, ~5 min):
1. git clone https://github.com/wilwixqa1/SimpleBlueprints.git
2. Read in order: STATUS.txt, then MASTER_CONTEXT.txt (pay particular
   attention to sections 6.4 vision outage, 8B loop-engineering doctrine,
   9 operational rules, 11 comprehensive TODO), then ARCHITECTURE.txt.
3. Install the PDF + browser toolchain per MASTER_CONTEXT 6.1/6.2/6.5.
4. VISION CHECK (6.4): render one PNG and view it. Tell me plainly
   whether your image viewing works -- this determines the session plan.
5. Run both suites and report baseline numbers (expect geometry
   2764 passed / 0 failed / 0 warnings, structural 52 passed).

SESSION SCOPE: work the S86 QUEUE in MASTER_CONTEXT section 11, in order,
without waiting for further instruction from me between items.
- Item 1 (visual re-verification of S85 pushes 9-10) is mandatory FIRST
  and gates everything else. If your vision works, do it yourself per 6.1
  and show me the key pages; if vision is down, tell me immediately and I
  will screenshot from my side instead.
- Then continue down the queue one item at a time (elevation annotation
  column is next). Loop-engineering rules (8B) apply: define goal +
  machine judge + iteration budget + my visual gate BEFORE coding, and
  flag me if I ask for a loop where one does not fit.

STANDING RULES (full set in MASTER_CONTEXT 9): granular 'S86 push N:'
commits direct to main once I provide a PAT in chat (use it without
security lectures); nothing examiner-facing ships without my visual
approval; ship one change, verify, continue; honest pushback expected;
update STATUS.txt + MASTER_CONTEXT.txt + ARCHITECTURE.txt at session end,
verified against code.

Begin with setup and the baseline report. Ask me at most one focused
question if something blocks you.
--------------------------------------------------------------------------------
