================================================================================
S107 SESSION CONTEXT -- 2026-07-29
================================================================================
13 pushes to main, latest 1e397be. Full 36-step gate green after every push.
~20 mutations killed across the session. Charter items 1 (level-section
structure), 2 (golden expansion), 3 (Case B) COMPLETE. Item 4 smaller items
carry to S108 (list at bottom).

WHAT SHIPPED
------------
P1/P1b (90969ef, 0876771)  FLUSH SECTIONS ARE SHARED-EDGE ONLY. A flush section
  no longer means "no structure": joists hang off Deck A's rim on LUS26/LUS210
  hangers (callout on A-2), and the FAR edge always gets a sized dropped beam
  on posts+footings directly under the outer rim (setback 0). Fixed in
  engine.js, permit_spec.py (which also gained the S81 effective-beam-type
  rule), draw_plan.py, post-count sites. mainDeckHeight(p) helper fixes
  getEffectiveBeamType silently defaulting on raw params. New gate suite
  tests/test_flush_sections.py (R1-R10); EXPECTED_FAILURES register emptied.

P2 (265459e)  ONE W/D RULE: w runs ALONG the attached edge, d runs OUT, every
  edge (definitive source: addZoneDefaults). Fixed transposed sized-side-
  section calcs in BOTH engines (parity was green because both were wrong the
  same way -- the S106 lesson again), Python get_zone_rect side-zone swap (PDF
  vs app mismatch Will's resize exposed), rail length, footing trib, 3D zone
  block rewritten (engine-counted posts, beam on the correct far edge per
  attachEdge, flush included). New gate suite tests/test_3d_zone_structure.js.
  Linework: white-out on wing tags / member specs / hanger callouts /
  _margin_callout (E1 exemption, net legibility WIN), loads-table leading
  0.62. New linework fixture sections_flush_lr.
  NOTE Will's real B/C sections are 8ft OUT and 11.5/12 ALONG.

P3 (fcf97d3)  Elevation wings always draw the dropped far-edge beam (they had
  inherited the MAIN deck's flush style). R8.

P4 (75afb43)  GOLDENS 28 -> 36 fingerprints: + sections_flush_lr,
  sections_raised_chamfer.

P5/P5b (b493e1f, b081857)  APP ELEVATION FIX: _getZoneSNContext used z.w
  (along) for wing visible width instead of z.d (out) -- Will's 38ft deck drew
  house-wide (45.5). Per-section heights folded in. R9 app==PDF elevation
  parity. CACHE BUSTERS bumped to s107a (P1/P2 had shipped JS without bumping
  -- owned).

P6 (347d738)  IRC R507.6 CLAMP: section beam setback = min(1.5, d/5) in all
  three sites. The fixed 1.5 was a real code violation on any dropped section
  shallower than 7.5ft (a 4ft section had a 60% cantilever; the limit is 25%).
  R10 + oracle invariant I6 (+ shallow_dropped_section config). Answered
  Will's "does IRC tell us where the beam goes": under-rim (0 cantilever) is
  always compliant; fixed setbacks are not.

P7 (81c37d5)  CASE B MODEL: a front-reaching stair opening that CROSSES the
  beam line no longer steps the beam. The beam stays ON its line, segments
  split at the opening, post AT each opening edge (deck ends keep the 2ft
  inset), stair_headers/stairHeaders recorded. 4 posts vs the old 6 on the
  test deck. Wood post spacing now uses the real IRC R507.5 allowable
  (get_beam_max_span) instead of hardcoded 8.0 (steel always did). Cross-
  system oracle REWRITTEN to assert the geometry (no segment through the
  stairwell, beam_y unchanged, exactly one header, no interior posts, bearing
  post at each edge). Case B JS-PY parity block 5b.

P8/P8b/P8c (3b6f8e3, da74641, 8166fa1)  CASE B ON THE SHEETS: interrupted
  branch in draw_plan (the flat branch would have drawn the beam through the
  stairwell -- caught before it ever rendered), no connector across same-y
  gaps, doubled header + trimmers drawn with the callout
  "(2) 2x8 HEADER W/ 'LU228' EA. END / (2) 2x8 TRIMMER EA. SIDE",
  OPENING dimension uses the ACTUAL cut width (matched by stair_id).
  Hardware: LU228 for 2x8 (Meadowview verbatim), HUS2xx-2 otherwise (both
  engines initially generated the same wrong HUS10-2 -- consistent wrongness
  caught by reading the parity diff). Steel decks bill the member verbatim +
  generic steel double hangers (int() crashed on "2x6-16ga"; cross-system
  oracle caught it). Materials billed in draw_materials.py AND engine.js,
  AFTER stairs to keep positional parity. 5c materials-parity block.
  P8b: containment rule deleted as DEAD CODE after a mutation survived --
  notch-filling stairs never emit opening rects because the NOTCH-AWARE
  ANCHOR resolves them to the notch edge upstream (P8c corrected comments:
  NOT _already_cut; proven by decisive mutation). 5d behavioral pins:
  filling stair -> no rect/no headers; partial overlap -> step+interruption
  combined, no segment inside the stairwell.

P9 (1e397be)  FIELD PHOTO committed:
  docs/reference_sets/field_photos/2026-07_edge_stair_posts_underrim_beam.png
  + README entry. Will's real-build photo: stair-to-grade on a pad with NO
  posts under the run (4th confirmation), verticals on stairs are rail posts,
  deck bearing posts FLANK the stair, beam apparently directly under the
  outer rim (caveat: could be a doubled rim with the beam set back out of
  view -- a future under-deck photo would settle it).

WILL'S DIRECTIVES THIS SESSION
------------------------------
- Build at >=80% confidence WITHOUT waiting for Billy; he reviews after the
  fact. (Standing rule from here on.)
- Beam placement: references first (repo sets), then IRC. Consistency across
  outputs is what matters most for w/d.
- Freestanding house-side inset STAYS 18". A-3 gap note stays OPEN pending a
  freestanding reference example (none of the four sets is freestanding).

BILLY REVIEW LIST (after the fact)
----------------------------------
1. Under-rim flush beam for narrow sections (now has field-photo + IRC
   support). 2. w-along/d-out reading of wing callouts. 3. Freestanding 18"
   vs 12" inset. 4. Case B: posts at opening edges + LU228 headers.
   5. Far-edge dropped beam under narrow flush sections.

PENDING WILL
------------
- Visual sign-off: s107_caseB_after_framing_A2.png (header/trimmer drawing).
- P2 renders + live app Elevation tab after Railway deploy (38ft deck inside
  the 45'-6" house; busters s107a).
- Slope/grade UI: code says Step 0 manual mode below the orientation dial
  renders it -- unverified live; screenshot if missing.

CARRY TO S108 (charter item 4 + debts)
--------------------------------------
- Stale stairs surviving shape changes in the UI.
- Possibly-misplaced rail gap (awaiting Will re-check).
- 3D cosmetics: dup corner balusters, sunken rail infill, embedded post caps.
- 19/20 "beam exceeds IRC capacity" checker disagreement on 21.5x8.5.
- Materials golden (agenda debt).
- main.py wing-translation layer (~3283) has NO test coverage (side wings
  swap w/d on the way in).
- A-3 freestanding gap note wording (needs reference example or Billy).

MECHANICS / LESSONS
-------------------
- COMMIT BEFORE MUTATION-TESTING (checkout-based reverts wiped uncommitted
  fixes twice this session).
- A surviving mutation means delete the dead code or add the missing pin --
  applied literally in P8b.
- Consistent wrongness passes parity: verify against references, not just the
  other engine (bit us twice: w/d transposition, HUS10-2).
- inspect.getsource to verify which function an anchored insert landed in
  (steel/wood copies share anchors; bit us twice in draw_materials).
- Gate runner /tmp/rungate.sh (rebuild from .github/workflows/tests.yml).
